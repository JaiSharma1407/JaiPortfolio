import React from 'react';
import Title from "../Title/Title";
import classes from './WebProjects.css';
const webProjects  = props => (
    <div className={classes.WebProject}>
        <Title tagline="Work I've done that resides out there, somewhere in the vast virtual space of the Internet.">Web</Title>
    </div>
);

export default webProjects;